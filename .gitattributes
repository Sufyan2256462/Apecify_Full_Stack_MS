# Auto detect text files and perform LF normalization
* text=auto

# These files are text and should be normalized (convert to LF)
*.css text
*.html text
*.js text
*.json text
*.md text
*.txt text
*.xml text
*.yml text
*.yaml text

# These files are binary and should not be modified
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.ico binary
*.eot binary
*.ttf binary
*.woff binary
*.woff2 binary
*.mp3 binary
*.wav binary
*.mp4 binary
*.mov binary
*.avi binary

# Explicitly declare files that should always have LF line endings on checkout
*.sh text eol=lf

# Don't process line endings for node_modules
node_modules/ -text
server/node_modules/ -text
client/node_modules/ -text

# Don't process line endings for package-lock.json (it should maintain exact formatting)
package-lock.json -text
server/package-lock.json -text
client/package-lock.json -text

# Don't process line endings for uploaded files
uploads/ -text
server/uploads/ -text
client/public/ -text
